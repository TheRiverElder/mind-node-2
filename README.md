# MindNode2

这是一个点线编辑器，创建点，然后用线将其连接，用于梳理内容。

# 更新计划

### 批量修改

等待格式更新到v3后再考虑支持。

### 存储时候确认是否会不小心覆盖数据

正在进行。

### 格式复制分组

等待格式更新到v3后通过style字段支持。

### 对齐网格

最后再考虑此功能。


### 自动换行

等待格式更新到v3后通过width字段支持。
