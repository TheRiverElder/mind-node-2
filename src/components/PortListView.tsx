import "../styles/PortListView.css";

export default function PortListView(props: {}) {
    return (
        <div className="PortListView">
            {/* Add your port list view components here */}
        </div>
    );
}